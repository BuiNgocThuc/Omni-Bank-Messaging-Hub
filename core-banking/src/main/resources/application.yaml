server:
  port: 8084
spring:
  application:
    name: core-banking-service

  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/FREEPDB1
    username: core_banking
    password: core_banking123
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true
        default_schema: CORE_BANKING

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: manual
        prefetch: 1

logging:
  level:
    org.hibernate.SQL: DEBUG
